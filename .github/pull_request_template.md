## What this changes

<!-- One or two sentences. If there is an issue: Fixes #123 -->

## How it was tested

<!-- Which VS Code version, and what you actually clicked. `F5` opens a second window with the
extension loaded. The CI run of this pull request also uploads an installable .vsix. -->

## Checklist

- [ ] `npm run check` passes
- [ ] Tried on a single item **and** on a multi-selection, on a file **and** on a folder
- [ ] `CHANGELOG.md` has an entry, if the change is one a user would notice
- [ ] `version` in `package.json` is untouched — bumping it is how a release is cut, and merging a
      bump publishes to the Marketplace
